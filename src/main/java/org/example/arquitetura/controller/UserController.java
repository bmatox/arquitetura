package org.example.arquitetura.controller;

import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.bind.annotation.RequestMapping;

@RestController
@RequestMapping("/users") // Define a URL base para os endpoints de usuário
public class UserController {
    // Aqui você vai adicionar os métodos para GET, POST, PUT, DELETE, etc.
}